<!-- Generated from docs/intake.md by kickoff v0.1.0. Edit the intake, not this file. -->

# MapDataStructures — Decisions

One line per decision. Newest at the bottom. Reasons come from the intake; where it gave none, the reason is "chosen in intake" and can be filled in later.

| # | Decision | Why | Source |
|---|---|---|---|
| D1 | Stack: TypeScript, Astro, no backend, no database, no data layer, CSS Modules, tests with Vitest and Playwright, bun | chosen in intake | Section 8 |
| D2 | Code lives on GitHub, public, licensed MIT | chosen in intake | Section 9 |
| D3 | Runs on Netlify; environments: production | chosen in intake | Section 10 |
| D4 | Default branch protected: false (differs from the default true) | chosen in intake | Section 9 |
| D5 | Database backup before every migration: false (differs from the default true) | chosen in intake | Section 9 |
| D6 | Handoff docs: true (differs from the default false) | chosen in intake | Section 9 |
| D7 | Must use: open-source libraries only | per Section 7: drawing, layout, and file generation run inside the app with open-source libraries | Section 12 |
| D8 | Must avoid: paid services | budget is free tiers only | Section 12 |

---

## Added after the build

| # | Decision | Why | Source |
|---|---|---|---|
| D9 | Phase 0.3 resolved to settings only: no ECC files installed for this project | ECC is already installed at user level, all 20 rule families and every skill this project's CLAUDE.md names; a project-local install would have duplicated 359 files and added about 60 unrelated agents | `/project-init`, 2026-09-18 |
| D10 | Permissions live in `.claude/settings.json`, shaped for bun, not npm | the ECC stack mapping's allowlist assumes npm and npx; this project runs bun, so the mapping's entries would never match | `/project-init`, 2026-09-18 |
| D11 | Creating the GitHub remote and editing the repo are denied to agents | the runbook reserves Phase 0 for a person because it needs account decisions; the deny list makes that instruction enforceable rather than advisory | `/project-init`, 2026-09-18 |
| D12 | Scaffolded with `bun create astro@latest . -- --template minimal --no-git`, but into a temporary subdirectory whose files were then moved into the root; package name `map-data-structures` | the scaffolder refuses a non-empty directory and, run non-interactively, invents a new folder rather than using `.`; moving the files in is what keeps `docs/`, `CLAUDE.md`, `LICENSE` and the existing `.gitignore` intact. Astro's generated `.gitignore` was merged into the existing one, not substituted. Confirms the flags the task file flagged as a guess | Task 01, 2026-09-18 |
| D13 | CI is `oven-sh/setup-bun@v2` with `bun install --frozen-lockfile` and `bun run test`, triggered on push to any branch but `main` and on every pull request | the task file's proposal, confirmed as written: it is the action Oven publishes, and the frozen install is what makes the committed `bun.lock` load-bearing. `bun run test` never `bun test`, which would run bun's own runner and skip Vitest | Task 01, 2026-09-18 |
| D14 | Provisional JSON shape: `{ title, nodes: [{ id, label, type }], edges: [{ from, to, label }] }` | the data model in `docs/ARCHITECTURE.md` names the fields but not the edge's two endpoints; `from` and `to` read as the direction the line is drawn. Provisional on purpose — Task 02 owns the Zod schema and the published shape, and is free to rename before anything is public | Task 01, 2026-09-18 |
| D15 | The design core's types live in `src/lib/design.types.ts` and are named `Design`, `DesignNode`, `DesignEdge` | the types outlive `parseDesign`, which Task 02 replaces with a validating `loadDesign`; keeping them in their own module means the schema, the layout and the four exporters share one definition. The prefix is because `Node` is a DOM global and a page module importing both would silently get the wrong one | Task 01, 2026-09-18 |
| D16 | Prettier is configured to the Astro scaffold's own style (2 spaces, single quotes, width 90) with `prettier-plugin-astro`, and does not format `docs/` or `.claude/` | the `setup-pre-commit` skill's defaults (tabs off, double quotes, width 80) would have rewritten every file the scaffold wrote; the plugin is the only way Prettier can read `.astro` at all. `docs/` is generated from the intake and `.claude/` is owned by the pipeline, so formatting either would churn files that get rewritten anyway | Task 01, 2026-09-18 |
| D17 | TypeScript itself is not a dependency yet, so nothing type-checks: no `astro check`, and no typecheck step in the pre-commit hook | Task 01 was to keep the dependency list to Astro, Vitest and the formatter, and Vitest transpiles without checking types. `tsconfig.json` does extend `astro/tsconfigs/strict`, so the settings are in place for whichever later task adds `typescript` and `@astrojs/check` | Task 01, 2026-09-18 |
| D18 | D14 is confirmed as final, not renamed: a design is `{ title, nodes: [{ id, label, type }], edges: [{ from, to, label }] }` | nothing is published yet, so this was the last chance to rename `from`/`to`, and nothing better came up: they read as the direction the line is drawn, they are what the walking skeleton, its README example and its tests already say, and a rename would have cost Task 09 and every reader of the repo more than it bought | Task 02, 2026-09-18 |
| D19 | Every field is required and no string may be empty; a property the schema does not name is ignored rather than rejected; a design with no nodes and no edges is valid | a node without a label is a blank box and an edge without a label is an unexplained line, so neither is worth drawing, and an empty `id` could not be named by an edge. Extra keys are ignored because a file exported from somewhere else may carry its own, and refusing it would be unfriendly with nothing gained. An empty design is a legitimate thing to describe and draws as an empty picture rather than as a failure — the case Task 01's test already covered | Task 02, 2026-09-18 |
| D20 | Unique node ids, and edges that only name nodes the file defines, are enforced in a Zod refinement on the whole design, and are reached only once every field has passed | neither rule can be expressed on a field, and both are silent corruption rather than a visible error: duplicate ids resolve every edge to the later box, and a dangling edge used to throw from inside rendering with no field to point at. The staging is Zod's and it is right — a rule that reads the whole list of nodes cannot run over a `nodes` that is not a list of nodes yet — so a file with both kinds of fault reports its fields first and its cross-field faults on the way back through | Task 02, 2026-09-18 |
| D21 | The types in `src/lib/design.types.ts` stay hand-written rather than becoming `z.infer` of the schema | the types are the contract Task 09 publishes and the four exporters read; hand-written they stay readable and read-only, and Zod stays an implementation detail of validation instead of something every consumer has to import. `loadDesign`'s declared return type is the one place the two meet, so they cannot drift without the type-checker saying so once D17 closes | Task 02, 2026-09-18 |
| D22 | `loadDesign` throws `DesignSyntaxError` or `DesignSchemaError`, both `DesignLoadError` with a `code`; the first keeps `JSON.parse`'s message and the original error as `cause`, the second keeps Zod's `issues` untouched | Task 04 has to name the line or the field, and neither a position nor an issue path survives being flattened into a sentence. The loader does not choose which fault matters either — that is presentation, and it is Task 04's | Task 02, 2026-09-18 |
| D23 | The upload area is styled by a CSS Module at `src/styles/upload.module.css`, and its drag state is a `data-dragging` attribute the script toggles | D1 names CSS Modules, and a drop target that is not visible is not a drop target. The attribute is what lets the CSS own the hashed class name and the script own nothing but the state. The dragging cue changes the border's style as well as its colour, so it does not depend on telling two colours apart | Task 02, 2026-09-18 |
| D24 | The layout engine is `@dagrejs/dagre`, not elkjs and not a hand-written placement | confirmed with the user against the three options. Verified at the moment it was added rather than from the handoff: 3.1.1, MIT, published 2026-08-08, one dependency (`@dagrejs/graphlib` 4.0.5, also MIT), and it ships its own types so no `@types` package is needed. A layered directed-graph layout is exactly what "boxes and arrows in a flow" is, which is every use case in the PRD; elkjs is far more capable but is a compiled Java port, is large, and is EPL-2.0 against our MIT (D2); a hand-written placement adds nothing to the bundle but is more code to own and would not route edges well | Task 03, 2026-09-18 |
| D25 | v1 draws six kinds of node — service, database, queue, external, user, decision — each with its own silhouette and colour band, and anything else as a plain grey dashed rectangle | the kinds are the ones the owner's draw.io designs and the PRD's own examples use, and each silhouette is one a reader already knows (cylinder for a store, stadium for a queue, cut corner for something outside the system). Aliases resolve to a canonical kind — `db`, `datastore`, `store`, `cache` all draw as a database — so two files that spell the same concept differently still draw the same. The lookup is on a lowercased, trimmed copy of the type; the string the drawing *prints* is the one the file gave, untouched. `type` stays free text (D14, D18, D19), so the default is not a failure case but the ordinary one: an unrecognised type still draws, keeps its label, and prints its own type text under it, visibly a box drawn without a claim rather than a box pretending to be a service | Task 03, 2026-09-18 |
| D26 | Node text is estimated from character count rather than measured, and a node grows taller rather than wider once its label passes the text limit | the layout has to be pure so Tasks 05 to 08 can run it in a worker, in a test and inside a PDF exporter, none of which has a `canvas` to measure against. The estimate over-shoots on purpose: a box slightly too wide looks airy, one slightly too narrow clips its label, and a clipped label is a mislabeled node, which intake 5.2 rules out. Wrapping is what keeps a long label inside a readable box without ever truncating it — the box grows, nothing is dropped | Task 03, 2026-09-18 |
| D27 | The drawing is styled by a CSS Module at `src/styles/drawing.module.css`, reaching the SVG through `data-part`, `data-shape` and `data-kind` attributes, and `src/styles/drawing.module.test.ts` measures every colour band against WCAG AA | D1 names CSS Modules and D23 set the precedent: the SVG is built by a script at runtime, so the CSS owns the hashed class name and the script owns nothing but the state. The contrast test is there because "label text has readable contrast against its shape" is an acceptance criterion and a criterion nobody can run is a criterion that rots — it caught the first grey at 3.40:1, under AA and looking perfectly fine | Task 03, 2026-09-18 |
| D28 | The SVG carries a `<desc>` as well as a `<title>`, written by `src/lib/describeDrawing.ts` | `role="img"` makes the drawing one object to a screen reader, so a `<title>` alone gives a non-sighted reader "Order intake" and not one thing that is in it. Intake 5.2's promise — every node and edge, nothing dropped — should hold for them too, and the text it needs is already in the layout. Accessibility here is best effort by `docs/ARCHITECTURE.md`; this is the cheap part of it | Task 03, 2026-09-18 |
| D29 | The typecheck script is `bun run check`, which runs `astro check`; it runs in CI before the unit suite and in the pre-commit hook after lint-staged. `typescript` is pinned to `^6`, not 7 | D17 closes here. `astro check` covers `.astro` and `.ts` alike — verified by planting an error in each — which matters because the page's `<script>` block was the one place no test and no type-checker could see. TypeScript 7's native compiler does not expose the programmatic API `astro check` drives, and `@astrojs/check`'s peer range is `^5 \|\| ^6`; on 7.0.2 the command refuses to start. The hook cost is about 5.8s, warm or cold, taking a commit from roughly 3s to roughly 9s — kept, because the block it covers is the blind spot it was added for | Task 03, 2026-09-18 |
| D30 | Playwright's `webServer` builds and then serves `dist/` with `e2e/staticServer.ts`, a foreground static server, rather than with `astro preview` or `astro dev` | the intent of the recommendation stands — this site ships as static files (D1), so the walk should run against the build output Netlify serves — but its premise does not. On Astro 7.3.3 `astro preview` detaches with no flags at all: it prints its URL, returns exit 0 in about three seconds, and leaves a background process holding a lock file, which `astro preview status` reports as "(background)". Playwright watches the process it spawned, so it reads that as "Process from config.webServer exited early", and the orphaned server then collides with the next run — both observed. `astro dev` behaves the same. A foreground server over the same `dist/` has no lock, no detachment, and dies with the run that started it | Task 03, 2026-09-18 |
| D31 | `testDir` is set explicitly in `playwright.config.ts`, and `bun run test` stays Vitest-only with the walk on its own `test:e2e` script | Playwright's default `testMatch` is `**/*.@(spec\|test).?(c\|m)[jt]s?(x)`, which matches every Vitest file in `src/`; without `testDir` the first e2e run drives a browser through the unit suite. The mirror holds too — `vitest.config.ts` includes only `src/**/*.test.ts`, so keeping specs out of `src/` keeps Vitest clean by construction. The scripts stay separate because the pre-commit hook runs `bun run test`, and a hook that starts a browser on every commit is a hook that stops being run | Task 03, 2026-09-18 |
| D32 | Correcting D22's premise, not its design: `JSON.parse`'s message does **not** name a position in every browser this app targets | D22 and the loader's doc comment both claimed it did. That is true of V8 and false of JavaScriptCore, which gives no position, line or column — so the claim was wrong for Safari, a browser a desktop app plainly targets, and `loadDesign.test.ts` could never have caught it because Vitest runs on Node. D22's design stands untouched: the loader still throws structured errors carrying `JSON.parse`'s own message and the original error as `cause`, and Zod's issues untouched. What changes is what is claimed about them — the message and the cause are the contract, a position is a bonus some engines give. Task 04 owns what to say when there is no position to say it from; this row exists so Task 04 is not built on a false premise | Task 03, 2026-09-18 |
| D33 | The file input's value is cleared after every read | re-picking an identical file fires no `change` event at all, because the value has not changed, so a user who edits their JSON and picks the same path again is shown the previous drawing and the previous status line — both still claiming to describe the file they just chose. Clearing it makes every pick a change. The file is already read by that point, so nothing is lost | Task 03, 2026-09-18 |
| D34 | A self-edge is routed by `layout.ts` itself — out of the node's right border, around, and back into it — rather than from dagre's points | dagre keeps self-edges out of edge routing: it parks a stub in a lane beside the node and leaves the loop to the consumer, so passing its points through, as is right for every ordinary edge, drew a line and an arrowhead in empty space touching neither end. A quietly wrong drawing is the one outcome intake 5.2 rules out, and the schema accepts a self-edge (both of D20's refinements do), so it is a file a user can write. The loop and its label stay inside `NODE_SEPARATION` of the node, which is the lane dagre already reserves beside a node that loops on itself, so the loop cannot reach into a neighbour's space | Task 03, 2026-09-18 |
| D35 | An edge label wraps at the same text limit a node label does, and its plate grows taller | the asymmetry had no reason behind it: a node label wrapped (D26) and an edge label did not, so a 300-character edge label made a plate about 2,250px wide, and because the SVG scales to fit the page, every node and every other label was shrunk to near-illegibility to make room for one of them. Nothing was dropped, so no criterion failed — but the same input now makes a plate 230px wide and 11 lines tall with every character still in it. The layout carries the wrapped lines on the edge, the way it already did on the node, so the renderer does no text work of its own | Task 03, 2026-09-18 |
| D36 | The spoken description agrees its article with the sound of the type that follows: `an external`, but `a user` | the `<desc>` (D28) is the one part of the drawing that is read aloud, and it said "a external", "a user", "a unknown". The rule is the spoken one rather than the spelt one — a vowel *sound* takes `an`, and a `u` pronounced "you" does not. Because `type` is free text (D14, D18, D19) this is a heuristic and not a dictionary: it is right for every kind in the shape vocabulary and every alias of one, and it will get an initialism nobody has thought of wrong. That is the right trade for one word of a description; the alternative is a word list that pretends to know every noun a user might type | Task 03, 2026-09-18 |
| D37 | Every self-edge on a node gets its own lane: the n-th loop reaches `SELF_LOOP_EXTENT × n` past the border and meets that border further from its middle than the one inside it, and all of that node's label plates are stacked in one column past the widest loop | routing the loop from the node's box and nothing else (D34) made it the same loop every time, so two self-edges on one node came out as one route drawn twice with two plates on the same spot — and because a plate is opaque and the wider one is drawn last, the first label disappeared underneath it: three edges in the file, two on screen, which is exactly the quietly wrong drawing intake 5.2 rules out. The ordinal is all that was missing; dagre had been reserving the room all along. Stepping the band as well as the reach is what makes it read as two loops rather than one loop with a spare bar — otherwise both arms and both arrowheads land on the same pixels. The plates share one column rather than stepping out with their own loops because a plate that steps out sits across the loops outside it and, being opaque, cuts their lines. This also refines D34's account of the room: what dagre reserves is one lane per self-edge, each sized to that edge's own label, not the single `NODE_SEPARATION` gap D34 named — swept programmatically over a four-sibling rank carrying two loops, the widest plate ends at x 826 with the next node at 1006, and no plate, box or route point collides. A node with one self-edge is the first loop in a stack of one and is drawn exactly where it was | Task 03, 2026-09-18 |
| D38 | A syntax message names a line only when the engine gave a position, and the line and column are counted from the file's own text rather than read out of the engine's phrasing. With no position the message says so; an empty file is reported as empty and a file of raw bytes as binary, both from the file itself rather than from the engine | D32 said `JSON.parse` names no position on JavaScriptCore; measuring the engines showed it is narrower than that even on V8, which gives `Expected double-quoted property name in JSON at position 248 (line 8 column 1)` for a trailing comma inside an object and no position at all for `Unexpected token ']', …` or for a file that ends early. So the no-position path is the ordinary path, not Safari's corner, and it is reachable in Vitest and in the walk. Counting the line here rather than scraping `(line L column C)` is what makes one wording hold across every engine that gives a position — older V8 gives `position N` alone — and `describeSyntaxFault` takes the message text and the file text as arguments so a JavaScriptCore message is a one-line test on Node. The standing rule is that a line number is never inferred from nothing: a wrong pointer into a file the user wrote themselves costs them more than no pointer. What is said instead names the fault and offers a next step, and for the two cases the file answers better than the engine — nothing in it, or bytes no text file has — it says that | Task 04, 2026-09-18 |
| D39 | Narrowing D19: no string may be **blank**, meaning it must hold at least one character that is not a space. `requiredText` is `z.string().min(1, { abort: true }).regex(/\S/u, { abort: true })` | D19 justified "no string may be empty" as stopping a node that draws as a blank box, and `z.string().min(1)` admitted `"   "`, which draws exactly that box — so the rule under-delivered on its own reason and the rule is what moved. Rejecting a blank string is not trimming an accepted one: `"  Public API  "` still loads with its spaces intact, because the drawing shows what the file says, and a test pins that distinction so it cannot quietly erode. `min` aborts so an empty string reports one issue rather than two, which matters now that the panel shows one message per issue, and the two checks stay separate because "this is empty" and "this is only whitespace" are different things to be told. Decided in this cycle on purpose: Task 09 publishes the schema, and after that the rule is something other people's files depend on. Both checks are representable in a published JSON Schema, which a `refine` would not have been | Task 04, 2026-09-18 |
| D40 | A file is checked before it is opened, by name and media type, against the same rule the file input's `accept` states: a name ending in `.json`, or a media type that is JSON. Either signal is enough | `accept="application/json,.json"` filters the picker and nothing else, so a file dragged onto the page bypassed it and a dropped PNG was read as text and handed to `JSON.parse`, which put its first bytes into its own message. The check is its own module because it takes a name and a type rather than text, so it cannot live inside `loadDesign`. Either signal is enough because a browser's media type varies by operating system and a `.json` file arrives as `text/plain`, or as nothing at all, often enough to matter. The cost is a file full of JSON called `notes.txt`, refused with a message saying to rename it — the picker would have hidden it too, so the two at least agree. It is a first gate and never a guarantee: a binary file named `design.json` passes it and is caught by the parser, which is why the syntax message has a binary case of its own | Task 04, 2026-09-18 |
| D41 | Two polite live regions that take turns: `#upload-status` carries success, `#upload-problems` carries failure, each outcome clears the other, and both are in the page from the first paint. Neither is `role="alert"`. The success line now reads `order-intake.json is drawn below: 7 nodes, 6 edges.` | A region announces changes to itself and not its own arrival, so a panel built on failure could have been announced as nothing at all; keeping the container and changing its contents is what makes it speak. Two polite regions changing at once talk over each other, and a stale success sentence sitting above a list of errors is exactly the quiet wrongness intake 5.2 rules out — so on failure the status line is emptied and the panel is the only voice, and on success the panel is emptied and the line is. Polite rather than assertive because the user caused this by choosing a file and is waiting for the answer; interrupting buys nothing. The success wording changed for the Task 03 backlog item that nothing announced the drawing had changed: "loaded" said a file was read, "is drawn below" says a picture appeared and where | Task 04, 2026-09-18 |
| D42 | The panel lists at most ten problems and counts the rest in a sentence, and its heading says what *this pass* found rather than what is wrong with the file | D20 is right and cannot be defeated: Zod reaches the rules that span fields only once every field has passed, so a file with a malformed field and a dangling edge reports the field now and the edge next time. "Here is everything wrong with your file" is therefore a sentence this app cannot honestly say, and a user handed a fourth problem after fixing three should feel the app is working through their file rather than that it lied. The cap is separate and practical: a 120-node file with one systematic mistake produces one issue per node, and a thousand list items in the DOM help nobody. The remainder line promises the rest on the next load, which is true | Task 04, 2026-09-18 |
| D43 | Nothing the app did not write reaches the screen unbounded: engine messages and the ids a schema message quotes are stripped of control and format characters, collapsed, and cut to 120 code points with an ellipsis, and every message is set with `textContent` and never `innerHTML` | Both sources are the user's file coming back out. V8 quotes the first bytes of whatever it failed to parse, so a renamed PNG put raw bytes in the status region; a node id is as long as its author felt like making it, and the uniqueness message quotes it. Cutting by code point means a bounded string never ends in half a character, and stripping `Cf` as well as `Cc` takes out the zero-width and bidi characters that make one string read as another. This is the one place D26 and D35's "wrap, never truncate" is deliberately reversed: a label is the user's own text and all of it matters, an engine quoting a megabyte of a file is not. `textContent` is stated in a comment in both modules because a panel that prints a file's own text is exactly where someone later reaches for markup | Task 04, 2026-09-18 |
| D44 | Narrowing D38: a syntax position is read only from a clause anchored to the **end** of the engine's message — `(?:in\|after) JSON at position N`, optionally followed by `(line L column C)` — and `engineDetail` strips that same pattern rather than one of its own | D38's rule was right and its implementation leaked. A `JSON.parse` message holds at most two things worth reading, the engine's clause and a quotation of the file that failed, and the second is the user's own bytes coming back: V8's no-position form is `Unexpected token 'p', "position 9"... is not valid JSON`. An unanchored search for `position 123` therefore found *the file*, and a YAML config saved with a `.json` name and beginning `position 900:` was answered `Line 1, column 10` — a pointer no engine gave, into a file whose real fault is line 1, column 1. That is exactly what D32 and D38 forbid, and a wrong pointer costs a user more than none. The end anchor is what makes it safe rather than merely narrower: every engine that gives a position ends its message with it, and every quotation ends `is not valid JSON`, so a message can never end in a clause it is only quoting — which holds however wide a snippet V8 decides to echo, including a short file quoted whole, clause words and all. Both prepositions are in the pattern because V8 says `in JSON` for a fault inside the design and `after JSON` for anything following it; matching only `in JSON`, the obvious one-line fix, would have cost a file with junk after its design the line it already had. Sharing one pattern with `engineDetail` is what stops the two disagreeing: the round 1 defect announced itself as a sentence that named a column and then ended `… is not valid JSON`, because the strip did not fire on a shape the scrape had matched. The cost is that every snippet-form message now takes the no-position path, which is the honest path and was already the ordinary one under D38 | Task 04, 2026-09-18 |
| D45 | A dropped file is tested by dispatching a real `DragEvent` carrying a real `DataTransfer` built from the fixture's own bytes, through `UploadPage.drop`, and `dispatchEvent`'s return value is what pins `preventDefault` | `setInputFiles` drives the file input and the drop listeners are on `document`, so the picker cannot stand in for a drop — and the drop is the path that matters, because `accept` filters the picker and nothing else, which is the whole reason `looksLikeJsonFile` exists (D40). A test named for a drop that called `setInputFiles` left `watchForDroppedFiles` with no coverage at all. The events are built by hand rather than through Playwright's `dispatchEvent` so that the return value is readable: it is false exactly when a listener cancelled the default, and that is the only observable trace of `preventDefault` here, because a synthetic drop cannot make a real browser navigate to a file — an assertion that the page is still on `/` passes just as happily with the `preventDefault` deleted. Each of the five tests was checked against a mutation of the page rather than trusted: removing the read of `event.dataTransfer` fails three, removing `preventDefault` fails the cancellation test, removing the drag cue fails the cue test | Task 04, 2026-09-18 |
| D46 | `layout.ts` splits into four modules along two seams — `selfLoops.ts` (a node's n-th loop), `normaliseDrawing.ts` (the margin offset and the canvas), and `layout.types.ts` (the vocabulary all three share) — and `layout.ts` stays the single public door, re-exporting every name that moved rather than re-declaring it | 625 lines is inside `CLAUDE.md`'s 800 ceiling and well past the 200–400 that is meant to be typical, and the module was about to gain four more readers: Tasks 05 to 08 all consume `layoutDesign`, so a behaviour-preserving split was cheapest at the moment it had the fewest consumers it will ever have again. The seams were already there rather than invented — the self-edge group is one idea (D34, D37) reached through two calls from `readRoutedEdges`, and the normalisation group is `layoutDesign`'s last three steps, which know about coordinates and nothing about dagre. Both were needed to meet the 400-line mark: the self-edge module alone left 436. The single door is what let `layout.test.ts` stay byte-for-byte unchanged through the move, which is the whole reason the refactor is verifiable; a re-declared constant would have been two `SELF_LOOP_EXTENT`s that agree today and disagree the day one is tuned. `PreparedEdge` genuinely straddles the first seam — built by `readRoutedEdges`, read by four of the self-loop functions — and rather than either module importing the other it joined the shared vocabulary in `layout.types.ts`, on the same reasoning D15 used for `design.types.ts`: `selfLoop` returns a `LayoutEdge` and takes a `LayoutBox`, so some type crosses the seam whatever is done with `PreparedEdge`, and one types module leaves every dependency pointing one way with no import cycle, not even a type-only one. Purity and serialisability hold of all four modules, because an exporter hands the result across a boundary. Preservation was demonstrated rather than asserted: `layoutDesign`'s output over six designs, one of them a node carrying three loops, is byte-identical to the baseline taken on `main`, and five deliberate mutations of the extracted modules each turn `layout.test.ts` red | Chore: split `layout.ts`, 2026-09-18 |
| D47 | The drawing travels inside the exported `.md` as a fenced ` ```mermaid ` `flowchart TD` block, written as text with no new dependency | confirmed with the user against a linked image and against doing both: a fenced block is self-contained, travels with the file, and renders natively on GitHub and GitLab, which is where "drops into a repo or wiki" points; a linked image is a second file to carry and breaks the moment the `.md` moves. `TD` rather than `LR` because `layout.ts` lays dagre out with `rankdir: 'TB'`, so top-to-bottom is the direction the user just looked at in the preview. No package is added, and none is needed (D7, D8): emitting the block is writing text, not rendering a diagram. Rendering was verified rather than assumed — the generated blocks for every kind and for the ugly cases (a quote, a hash, a literal `-->`, a line break, brackets and braces in labels, an id with a space) were parsed and rendered by real Mermaid 10 and 11 in a throwaway browser run, and the labels come back out of the SVG character for character | Task 05, 2026-09-18 |
| D48 | `toMarkdown` takes the **layout** alone — `toMarkdown(layout)` — not the `toMarkdown(design, layout)` the task file sketched | `DesignLayout` already carries the title, both lists in file order, every label exactly as the file gave it, and each node's `type` and resolved `shape.kind`, which is all four of the things the file prints. Taking the design as well would be two sources for the same fields and one more way for the `.md` and the picture to disagree; the export's whole promise (intake 5.2) is that it shows what the preview shows, and the preview is drawn from the layout. The geometry genuinely is unused — Mermaid computes its own placement, so no route, box or plate is read — but the resolved kinds are not, which is why this reads the layout rather than the design. The same signature is what Tasks 06 to 08 will want, since those do need the geometry | Task 05, 2026-09-18 |
| D49 | The diagram mints its own node ids (`n0`, `n1`, … in file order) and keeps the user's ids in the table; every label is quoted, with `#` escaped to `#35;` before `"` is escaped to `#quot;`, and any line break becoming `<br/>`. Table cells escape `\` and then `\|`, and break lines the same way | ids and labels are free text that the schema only checks for non-blankness (D19, D39), so an id may hold a space or a bracket Mermaid will not take as an id, and a label may hold a quote, a hash, a brace or a literal `-->`. A block that silently fails to render fails intake 5.2 as surely as a dropped node does. The hash is escaped first because the escape for a quote is spelled with one — the other order turns every escaped quote back into text. Backslashes are escaped before pipes for the mirror reason: a label ending in `\` would otherwise escape the divider after it and merge two cells. Line breaks are converted rather than passed through because a raw newline ends a Mermaid statement and a Markdown row alike | Task 05, 2026-09-18 |
| D50 | Each node's silhouette is keyed off `shapeForType(node.type).kind` as the layout resolved it — service a rounded box, database a cylinder, queue a stadium, external a subroutine, user a hexagon, decision a rhombus, anything else a plain rectangle | re-deriving the mapping from the `type` string would be a second vocabulary beside `shapes.ts` (D25) and it would drift; keying off the kind the preview drew is what keeps the `.md` and the SVG from ever disagreeing about what a node is. `external` is drawn as Mermaid's subroutine `[[…]]` rather than falling back to a rectangle, because the preview gives it a cut corner of its own and a rectangle would make "outside the system" and "a type nobody has heard of" look identical. A test builds one node per `KNOWN_SHAPES` entry and fails if two kinds draw the same way or if any known kind falls through to the rectangle, so a seventh kind added to `shapes.ts` is caught here rather than exported quietly | Task 05, 2026-09-18 |
| D51 | A design with no nodes exports a sentence — "This design has no nodes, so there is nothing to draw." — instead of an empty fenced block; both tables still print their headings | an empty design is valid (D19) and must export something rather than fail, but an empty ` ```mermaid ` block is the one output that could render as an error banner where the file is pasted, and that could not be verified across both GitHub's and GitLab's renderers without shipping a dependency to do it. A sentence cannot fail to render and says the true thing. The table headings stay because a header-only GFM table is valid and keeps every export the same shape | Task 05, 2026-09-18 |
| D52 | The shared download helper is `downloadBlob(blob, fileName, doc)` — a `Blob`, not a string — with the file name built by `fileNameFor(title, extension)`, and the object URL revoked on a timer rather than in the same tick as the click | Markdown and HTML are text but PDF and Word are bytes, so a helper taking a string would be rewritten in Task 07; a `Blob` carries all four. It takes its `Document` the way `renderDrawing(layout, doc)` does, which keeps the DOM work in one named place and leaves the unit test on the pure part — this project's Vitest suite has no DOM environment at all, and the standing split is pure logic in Vitest, DOM in Playwright. The name rule lives beside the helper rather than inside `toMarkdown` because Tasks 06 to 08 need the same stem with a different extension: the title is trimmed, whitespace and the characters Windows reserves become single dashes, control and format characters are dropped, the stem is capped at 80 characters and never starts or ends in a dash or a dot, and a title that survives none of that — `***` — falls back to `design`. Trimming here is not a contradiction of D39: that rule is about what the drawing shows, and a file name is not text the design shows. Revoking is deferred because revoking in the same tick has been observed to cancel the download, and never revoking leaks the file for the life of the page | Task 05, 2026-09-18 |
| D53 | The page now holds the layout it drew, in one variable set by one function, and the Export button is in the page from the first paint and disabled until there is something to export — and disabled again the moment a file fails | the page held nothing before this: `showDrawing` computed design → layout → SVG and discarded both. The trap is the failure path — `showProblems` empties the drawing, and a page that did not also drop the held layout would offer a button that exports the previous design while an error panel is on screen, which is exactly the quiet wrongness intake 5.2 rules out. So the drawing, the held layout and what the buttons offer are set together in `holdDrawing`, and a Playwright test pins the cleared case. Present-and-disabled rather than absent, because a control that appears half way down a page rearranges it under a reader, and disabled rather than enabled-and-silent because a button that downloads an empty file is worse than one that waits. The layout is held rather than the design so the file and the picture cannot come from two different computations | Task 05, 2026-09-18 |
| D54 | A task file's `**Status:** done` marks that its acceptance criteria are checked and verified in the build cycle — every box ticked, `bun run test` passing, CI green — not that its branch has merged to `main`. The merge is a separate, later gate, owned by Sam | Jahmyr set Task 05's status to `done` before the merge and flagged it as a judgement call to be settled once, for Tasks 06-11. Tracking `done` by merge instead would mean a task that is fully verified and only waiting on the release gate reads `in progress`, which is a worse signal for whoever picks the frontier next — nothing further can be done to the task itself at that point. This also matches what was already practiced: Tasks 01-04 were each marked `done` on their own branch before their own merge, so this makes an existing convention explicit rather than introducing a new one | Task 05 release gate, 2026-09-18 |
| D55 | `toHtml` takes `toHtml(layout, doc)` — the layout D48 settled, plus the `Document` — and the module exports a pure `htmlPage(layout, drawing)` beside it: the whole page given the drawing already serialised | the task file sketched `toHtml(design, layout)`, but D48 settled the design half and said so for Tasks 06 to 08; the only open part was the `Document`, and `renderDrawing(layout, doc)` returns a DOM element rather than a string, so a purely pure `toHtml` could not call it. Taking the `Document` as an argument is the house pattern D52 spells out — `downloadBlob(blob, name, doc)` does the same — rather than reaching for a global. The second function is what makes the choice testable: this project's Vitest run has no DOM at all, so `htmlPage` is unit-tested with a stand-in drawing string in `toHtml.test.ts`, and everything DOM-shaped — that the serialised drawing is the preview's own element, that the file opens alone, that a markup label neither runs nor disappears — is asserted in `e2e/export.spec.ts` against a real browser. Writing a second, string-producing SVG renderer to dodge the `Document` was the alternative and was rejected: two renderers to keep in agreement is exactly what intake 5.2 forbids | Task 06, 2026-09-18 |
| D56 | User text in the exported page is **escaped, never stripped** — `&` first, then `<`, `>`, `"`, in one `escapeHtml` — and the drawing is made safe by serialising `renderDrawing`'s element rather than rebuilding it as a string | this is the first output the project produces that a browser will execute, so the two halves needed separate answers. The SVG half is already safe by construction: `renderDrawing` sets every label with `textContent` and never `innerHTML`, so a label reading `<script>` is text in the DOM, and reading `innerHTML` back off a holder escapes it on the way out — reading is the opposite of the writing D43 rules out, which is why `serialiseDrawing` says so where someone will later reach for markup. The exposure is what `toHtml` writes by hand — the `<title>`, the `<h1>`, the table cells — so those go through one named function rather than ad hoc at each call site, with `&` first because every other replacement writes one. Escaped rather than stripped because Task 05's Mermaid `strict` level stripped such a label away and the box drew empty; dropping a label the preview shows is the one thing intake 5.2 rules out, and the label keeps its spaces too (D39), which `white-space: pre-wrap` on the cells is what makes visible. A fixture whose labels are literally `<script>`, `<img onerror>` and `</style><script>` is exported, reopened from disk, and asserted to show each as text, run nothing, and leave exactly one `</style>` in the file — the one this app wrote | Task 06, 2026-09-18 |
| D57 | The exported page carries its own stylesheet as a string constant in `src/lib/exportStyles.ts`, a second copy of the drawing's colour bands, and `exportStyles.test.ts` reads `drawing.module.css` off disk to keep the two identical | criterion 3 is absolute — no stylesheet, script, font or image fetched from anywhere — and the drawing's colours live in a CSS Module whose class names are hashed at build time. An ordinary import of that file is rewritten by Astro to a URL, which is the fetch the criterion forbids; `?raw` and `?inline` both come back as an empty string under this project's Vitest (`css: false`), which would mean the built file was the only place the real stylesheet existed and the test passed on nothing. So the values are written out twice and the duplicate is made safe by a test rather than by care: it parses every `--shape-*` band and every route, arrowhead, plate and edge-label colour out of both files and fails the day one is re-tuned and the other is not, the same way `drawing.module.test.ts` already holds the contrast criterion. The selectors stay the `data-` attributes of D23, since the export embeds the preview's own SVG; only the hashed container class becomes a plain `.drawing`. It is printable too, per the task file's note that Task 07 may print this page — colour bands printed rather than dropped, and no row or drawing broken across two sheets | Task 06, 2026-09-18 |
| D58 | `straightLine` (`src/lib/layout.ts`) is parked again, now as **proven unreachable** rather than merely unhit, and is kept as a guard rather than corrected or deleted | it was parked through Task 05 because no exporter read `LayoutEdge.points`; Task 06 is the first that does, so it was named for a verdict here. The verdict is that its inputs cannot occur. Dagre ends its own layout with `assignNodeIntersects`, which unconditionally unshifts the source border's intersection and pushes the target border's onto every edge, so no edge comes back with fewer than two points and both ends are already on a border; `buildGraph` adds one dagre edge per design edge keyed by its index, so the lookup never misses; and `selfLoopSlots` gives every self-edge a slot, so a loop is drawn by `selfLoops.ts` and never read from dagre. Probing agreed — ten shaped graphs (parallel edges, two-cycles, self-loops, a complete graph, a fifty-node chain) and four hundred fuzzed ones, three points at the fewest. So the arrowhead-inside-a-box exposure does not exist, on screen or in the export. It is kept rather than deleted because it guards a library this module does not own, and deleting it would turn a future dagre change into an edge drawn with an empty `d` — invisible, which is worse than visibly wrong. It is left centre to centre rather than corrected because a correction to code nothing can reach could not be tested, and an untested correction is worth less than the note now beside it | Task 06, 2026-09-18 |
| D59 | The export row is a `role="group"` labelled `aria-label="Export the design"`, not a `<fieldset>`/`<legend>`, and every button in it is switched together by `holdDrawing` | one button needed no name; two do, because a set of controls that do one job in different formats reads as a set, and a screen reader says the group's name as focus enters it — so "Export Markdown" is heard as one of the exports rather than as a loose button somewhere down the page. `role="group"` rather than a fieldset because these are actions, not fields of a form, and a fieldset brings borders and padding that Task 09 would have to undo while the page around it is still deliberately unstyled. Decided now rather than piecemeal, so Tasks 07 and 08 add a button and nothing else. Both buttons are disabled together for D53's reason, and because they enable together a keyboard user tabs straight from one to the other, which a test pins; the fuller revisit of the row was already flagged for when it holds four | Task 06, 2026-09-18 |
| D60 | The exported page shows the drawing first and the two tables after it, and a design with no nodes gets D51's sentence in place of the drawing rather than an empty `<svg>` | order: the `.md` leads with its tables because Mermaid may not render where the file is pasted, so the tables are its guaranteed content; an `.html` always renders, and the page's whole point is the picture (intake 2.1), so the picture leads and the tables are the detail under it. Section order is not a 5.2 question — nothing is dropped either way — but it is worth choosing rather than inheriting. Emptiness: D51's reasoning was that an empty fenced block could render as an error banner where the file is pasted, which inline SVG cannot do, but the sentence is still the right answer and keeps the two formats saying the same thing. An empty canvas in a page someone was sent reads as a picture that failed to load, where the sentence says the true thing; both tables still print their headings, exactly as the `.md` does | Task 06, 2026-09-18 |
| D61 | Correction to D57: under this project's Vitest (`css: false`), `?inline` comes back as an empty string, but `?raw` does **not** — it comes back as the CSS-Modules proxy stub, where `typeof` reads `object`, `String()` throws `Cannot convert a Symbol value to a string`, and `.length` reads back an invented class name (`_length_f9673f`). D57's reasoning still holds and is stronger for it: a test against the raw import would have thrown rather than quietly passing on nothing, so `exportStyles.test.ts` reading `drawing.module.css` off disk was still the right call. Only the recorded fact about `?raw` was wrong, found by Jahmyr during Task 06's release-gate review and confirmed independently by Sam with a throwaway probe test under this project's Vitest config before recording it here | Task 06 release gate, 2026-09-18 |
| D62 | The PDF is drawn as **vector**, with `svg2pdf.js` 2.8.1 on `jspdf` 4.2.1, both MIT, and both confirmed with the user before either was added | the trade-off the task file asks for by name: vector keeps every label selectable, searchable and sharp at any zoom and the file small — the 15-node fixture exports at 65 KB, because jsPDF subsets the embedded font down to the glyphs actually used — where the raster route turns the labels into pixels that blur the moment a reader zooms in on the one they are trying to read, which pulls directly against "every label readable". The cost is two packages rather than one, a dependence on how much SVG svg2pdf covers, and the font question (D65), which the raster route would have dodged. The facts were checked at the moment of adding rather than taken from a handoff, as D24 set the precedent: `jspdf` 4.2.1, MIT, published 2026-03-17, three dependencies, ships its own types; `svg2pdf.js` 2.8.1, MIT, published 2026-08-31, four dependencies, ships its own types, peer `jspdf ^4 \|\| ^3 \|\| ^2`. Two routes were declined. `pdf-lib` had gone four years without a release. Printing the standalone page Task 06 built adds no dependency at all and was the cheapest thing on the list, but the task file's own sentence is that the button *downloads* a `.pdf` and criterion 4 says the walk checks *this download*: a print dialog is the user saving a file rather than the app handing one over, and Playwright's `waitForEvent('download')` never fires for it, so two criteria could not be met as written | Task 07, 2026-09-18 |
| D63 | `toPdf(layout, doc)` returns a `Promise<Blob>`, and the whole shape of the document is decided by a pure `pdfPlan(layout, measure)` in a module of its own — the unit tests are `pdfPlan.test.ts`, everything DOM-shaped is `e2e/exportPdf.spec.ts` | the task file sketched `toPdf(design, layout)`; D48 settled the design half for Tasks 06 to 08 and D55 settled the `Document`, so the only open part was where the seam goes. It goes further here than it did for `toHtml`, because a PDF has more decisions in it than a page does: `pdfPlan` returns every page and every piece of ink on it — the title, the drawing's rectangle, each wrapped line of each table cell — as plain data, so the ordering, the wrapping, the page count and the empty-design sentence are all pinned in Vitest, which has no DOM at all. `toPdf` is then the part that cannot be unit-tested at all and is left with nothing to decide: it opens jsPDF, hands `pdfPlan` a measuring function built from the embedded font's own metrics, and puts ink where the plan says. It is a promise because svg2pdf is asynchronous and because the library and the font are fetched on demand; the page awaits it, and takes the design into a local first so that choosing another file while a PDF is still being written cannot save the last design's picture under this design's name | Task 07, 2026-09-18 |
| D64 | The drawing is rendered fresh, put into the page off-screen inside a holder carrying the preview's own class, asked what its styles resolved to, and those answers are written onto the elements as inline styles before svg2pdf reads them | svg2pdf does not use `getComputedStyle`. It collects `<style>` elements found *inside the SVG* and matches selectors itself, so the app's CSS Module never reaches it — and even if it did, every colour in `drawing.module.css` is a `var(--shape-*)` that svg2pdf would hand to its colour parser unresolved. The obvious alternative was to give the export a stylesheet of its own with the colours written out literally, which is a second copy of the palette and the thing D57 had to build a test to make safe. Asking the browser instead means the PDF's colours *are* the preview's own stylesheet as the browser resolved it, so there is no second copy to keep in step and no test needed to hold one. It has to be in the page because a detached element has no resolved styles, so the holder is `position: fixed` far off to the left, `aria-hidden`, and removed in a `finally` whether the conversion worked or not | Task 07, 2026-09-18 |
| D65 | The exported PDF embeds **Roboto Regular** (SIL Open Font License 1.1) as base64 in `src/lib/fonts/robotoRegular.ts`, dynamically imported, and every piece of text in the file — the drawing's included — is forced to it at `font-weight: normal` | jsPDF's built-in faces are the PDF standard set, which is Latin-1: the project's own `markup-labels.json` fixture has a Greek label, and with a built-in face it comes out of the file as `±Á±³³µ»¯µÂ`. A label shown as the wrong glyphs is a label dropped, which is the one thing intake 5.2 rules out, so a font had to travel in the app. Roboto because it is already the first named face in the drawing's own stack, so the file reads like the preview; because it covers Latin, Greek and Cyrillic; because at 155 KB it is the smallest face that covers all three; and because the OFL permits embedding in a document, which satisfies D7. **This is a licence and a bundle cost being raised rather than taken quietly**: 212 KB of base64 in the repository, loaded only by a visitor who actually exports a PDF, alongside jsPDF's own 400 KB. **It does not cover every script**: a label in Chinese, Japanese, Korean, Arabic, Hebrew or an Indic script has no glyph and is left out of the PDF, though the other three exports still show it — no face of this size covers those, so the gap belongs to the format rather than to the choice. The weight is normalised because svg2pdf turns any weight that is not 400 or 700 into a style name of its own — a label styled `font-weight: 550` is asked for as `550normal` — and jsPDF answers a style it has never been given by silently falling back to Times, which is Latin-1 again. That defect was in the first working build of this export and the tables beside the drawing were correct while the drawing was not | Task 07, 2026-09-18 |
| D66 | The page is US Letter, 612 × 792 points, with a 48-point margin; the drawing goes on the first page under the title, scaled to fit and never scaled up; the tables follow and repeat their column headings on every page they run onto; and a design with no nodes gets D51's sentence | Letter rather than A4 because the owner is in the United States and this is a document to print or attach. The drawing is scaled to fit the page rather than tiled across several, which means a forty-node design prints small — vector is what saves it, since it stays sharp at any zoom, and both tables carry every label at full size whatever the drawing is scaled to. Repeating the column headings is what makes page three readable on its own. The sentence for an empty design keeps the three formats saying the same thing (D51, D60), and a Vitest test asserts the PDF's copy of it is byte-for-byte the one `toMarkdown` writes, so a third literal copy cannot drift. The output is left uncompressed, which costs a few kilobytes and keeps the file readable to the test that reads its text back out | Task 07, 2026-09-18 |
| D67 | Criterion 11.1 is held by a **recorded measurement** plus a deliberately loose guard in the walk: the timed tests assert the export finishes inside 15 seconds, and the real numbers are in the handoff | a hard wall-clock assertion tight enough to be interesting is also tight enough to go red on a busy shared runner, and a flaky test is worth less than no test. The measurements on the build machine (Windows 11, bun 1.4.2, Chromium via Playwright, `dist/` served from localhost) are 134 ms for `platform-overview.json` at 15 nodes and 16 edges on the first click, 61-81 ms warm, and 181 ms for `estate-sweep.json` at 40 nodes and 46 edges, 97-119 ms warm. Two orders of magnitude of headroom under "a few seconds" is what makes the loose guard defensible: what it still catches is an export that has stopped finishing at all, and the number it would have to regress by to go red is not a number a real change produces quietly. Both fixtures are new, because there was none at the owner's scale — `order-intake.json` at 7 nodes was the largest valid design in `e2e/fixtures/` | Task 07, 2026-09-18 |
| D68 | The PDF's text is read back out of the produced bytes by `e2e/pdfText.ts`, written by hand rather than with a PDF library, and the walk also asserts which **faces** the file draws with; the PDF block lives in `e2e/exportPdf.spec.ts` rather than in `export.spec.ts` | "every label readable" cannot be checked by looking at what went in, and it cannot be checked by eye. jsPDF writes its content streams and its `/ToUnicode` CMaps uncompressed and plainly, so reading them is short enough to keep in the repository; a PDF library would be a dependency added for the test suite alone, on a task whose dependency gate was deliberate. It is a test oracle, so it was checked against one: its output for the fixtures agrees with `pdftotext` (xpdf 4.06), and the suite carries the hand-written one because a system binary present on the build machine and absent on the CI runner is a test that passes locally and fails in CI. Reading the text back is not enough on its own — the tables can be right while the drawing beside them is wrong, which is exactly what the `550normal` fallback did — so `pdfFonts` reads the `Tf` operators and the walk asserts the only face used anywhere is the one embedded. The separate spec file is because a third format took `export.spec.ts` to 829 lines, past the 800 `CLAUDE.md` sets, and because none of this machinery is shared with the two text formats; Task 08 should follow this pattern rather than the old one | Task 07, 2026-09-18 |
| D69 | A character the embedded face has no glyph for is replaced with one visible mark, `■` (U+25A0), in the drawing and in the tables alike, and the page then says how many were replaced and that the Markdown and HTML exports still carry them. **This corrects D65's enumerated list**: the gap is not only CJK, Arabic, Hebrew and Indic, because the face has no `→`, `←`, `↔`, `⇒`, `✓`, `✗` or `∈` either, so it reaches ordinary English designs | jsPDF writes *nothing at all* for a character it cannot map — not in the picture and not in the tables, so no copy of it survives anywhere in the file while the export still reports success. `Gateway → Queue` arrived as `Gateway  Queue`, and `API gateway (東京)` as `API gateway ()`, which reads as a finished label: a reader cannot tell a region name was ever there, and neither can the person who exported it. That is intake 5.2's "mislabeled" rather than an honest limit. Embedding a face that covers every script is megabytes and was declined; what was wrong was the silence, not the coverage. The mark is the **filled** square because the face has no glyph for the hollow `□` either, so a hollow placeholder would have been dropped in its turn and left the file saying nothing — a unit test asserts the face can draw whatever the mark is set to. The marking runs before the document is measured, so the tables wrap around what is actually written, and one mark per character means the boxes, the routes and the canvas stay exactly the ones the preview used. The sentence is said only when something was lost: announcing a finished download is D41's question, parked for all four exports at once | Task 07, 2026-09-18 |
| D70 | What the face can draw is read out of the font's own `cmap` at export time in `src/lib/fontCoverage.ts`, by jsPDF's rule rather than OpenType's, and is never written down as a list of ranges | A hand-kept list of covered characters is a second copy of the font, and it drifts the first time the font is regenerated or subset; reading the bytes the export actually embeds cannot drift. The rule is taken from jsPDF's own source rather than the specification, because what matters is not what the font could express to a capable renderer but what jsPDF will look up: the first platform 3 encoding 1 format 4 subtable or the first platform 0 format 4 one and no other format, glyph 0 counted as nothing drawn, and nothing above the basic plane, since jsPDF looks a character up one UTF-16 code unit at a time. Measured at 0.2 to 0.9 ms per export against an export of 110 to 160 ms, so it is not cached — a cache here would be module-level mutable state bought for nothing. A font this cannot read makes the export throw, and the page says `EXPORT_FAILED` in its own words (D43); reporting an empty coverage instead would mark every character in the design and quietly produce a document of nothing but placeholders | Task 07, 2026-09-18 |
| D71 | The exported PDF carries `/Title`, the design's own title, and `/Lang`, which is `en`, and the title is taken from the layout before the marking | Round one produced a file whose Info dictionary held nothing but `Producer: jsPDF 4.2.1`: the title was ink on page 1 and nothing else, so a viewer showed the file name in its window bar and a screen reader had no document title or language to announce. Both are a single call. The title is the one the file gave rather than the marked one, because metadata is written into a dictionary rather than drawn with a font — jsPDF writes a non-ASCII title as UTF-16 with a byte-order mark — so it carries characters the pages themselves have to mark. The language is fixed at `en` rather than guessed from the design, because the document's own furniture (its headings, its column names and the sentence an empty design gets) is English whatever language the design is written in, and the page that builds the file declares `lang="en"` too. Task 08's `.docx` wants the same habit | Task 07, 2026-09-18 |
| D72 | A control character in a label is marked like any other character the font cannot draw, and the font is not consulted about one; the newline alone is spared | jsPDF does not skip a control character the way it skips an uncovered glyph — it ends the string there. Measured on jsPDF 4.2.1 with the embedded face, `AAA<c>ZZZ` writes `AAA` for U+0009, U+000B, U+000C, U+000D, U+0001, U+001F, U+007F, U+0085 and U+009F, and `getTextWidth` still charges the column for text that is never drawn. The tab was spared until now on the premise that `pdfPlan` breaks a label's lines on it, and that premise was false: `wrap` splits a cell on `'\n'` and nothing else, and `splitWords` breaks on a space rather than on whitespace, so `Alpha\tBravo` reached the Nodes table as `Alpha` — unmarked, uncounted and reported as a success, which is the silent loss D69 exists to end, arrived at through another door. The coverage answer is not consulted for a control character because it misleads: Roboto Regular maps U+0000, U+0002 and U+000D, as many faces do, and jsPDF truncates at them all the same. The newline is the one exception and its safety is checked rather than assumed — `pdfPlan`'s `wrap` splits a cell on it before a line is measured, and svg2pdf removes newlines from a text element before jsPDF sees it — so a label written on two lines keeps them instead of gaining a square. The cost is that a `\r\n` inside a label now shows a mark at the end of its first line and is counted: a lone `\r` truncates like any other control character, and a rule that asked where in the string the character sat would be a second kind of answer to the same question | Task 07, 2026-09-18 |
| D73 | The Word file is built with `docx` (dolanmiu/docx) 9.7.1, confirmed with the user before it was added | the gate Task 03 and Task 07 went through, opened the same way and answered the same way. Verified at the moment it was added rather than from the handoff: 9.7.1, MIT, `LICENSE` in the package, five dependencies (`hash.js`, `jszip`, `nanoid`, `xml`, `xml-js`), its own `index.d.ts`, and an ESM entry with no bare imports left in it, so Astro's build takes it whole and `bun run build` passes. It builds real `Table`/`TableRow`/`TableCell` elements, which is what criterion 2's "editable text" asks for, and `Packer.toBlob(file)` hands back exactly the `Blob` `downloadBlob` takes, so D52's helper is unchanged for a fourth time and needed no edit. The rejected options were `html-docx-js` (last published 2016, jszip 2.x, no types, and its `altChunk` output is materially weaker in LibreOffice, which criterion 1 names), `docx-templates` (needs a binary Word template committed to the repository, and this document's structure is dynamic), `officegen` (Node-only, against D1's browser-only rule) and hand-writing the OOXML over a zip writer (full control, but every part has to be right at once and Word's failure mode on any one of them is to refuse the file) | Task 08, 2026-09-18 |
| D74 | `toDocx(layout, doc)` returns a `Promise<DocxExport>`, and the whole shape of the document is decided by a pure `docxPlan(layout)` in a module of its own; the drawing goes in as a **raster** PNG, painted at three times the size it is placed at | D63's split, kept for D63's reason: this project's Vitest run has no DOM, so the more of the document that is decided in a pure function the more of it a unit test can hold. `docxPlan.test.ts` holds the page, both tables, the column widths, the placed size and the character rule; `e2e/exportWord.spec.ts` holds everything that needs a browser. The raster is not a preference: `docx` types an image as `jpg`, `png`, `gif` or `bmp`, and its SVG variant requires a raster fallback of one of those regardless, because Word's own SVG support varies by version, so D62's vector is not on offer here and a PNG is produced either way. Three times the placed size is 288 DPI on the page, and because the placed size is capped at one page the canvas is bounded at about 4.3 megapixels whatever the design: measured on a 200-node, 260-edge design the export took 558 ms and the page's heap did not move. Scaling the *drawing's* own size instead is what would have needed a pixel budget and a canvas-dimension cap | Task 08, 2026-09-18 |
| D75 | The Word page is US Letter **portrait**, 12,240 x 15,840 DXA, with a 1,440 DXA (one inch) margin on every side; the drawing is scaled to fit the content box less an inch for the title, and never scaled up | D66 said the Word document should choose its own page rather than inherit the PDF's, and OOXML measures in DXA (twentieths of a point) and EMU rather than points, so nothing was inherited. `docx` defaults to A4, which is the wrong paper for the owner. An inch of margin is Word's own default and the shape a document someone is going to edit is expected to have, which is the whole reason this export exists. Landscape was considered and measured rather than waved away: the drawings this app produces are usually taller than wide -- `markup-labels` is 423 x 660, `order-intake` 570 x 766, `estate-sweep` 1060 x 6480 -- so turning the page would shrink the picture in three of the four fixtures to help it in one (`platform-overview`, 1541 x 1082). The cost of portrait is the same label-size limit the PDF has, measured off the produced files and reported in the handoff for the cycle that fixes it | Task 08, 2026-09-18 |
| D76 | What the Word export marks is exactly what would stop the file opening -- every character XML 1.0 has no room for -- and nothing else: a tab is kept, every line ending becomes a real `w:br`, and the mark is D69's square, counted and said out loud | measured against `docx` 9.7.1 rather than assumed, because Task 07 lost two rounds to exactly this class. The library escapes `&`, `<`, `>` and `"` and writes everything else through verbatim, so a U+0001 in a label lands raw in `word/document.xml`; XML 1.0 forbids U+0000-U+0008, U+000B, U+000C and U+000E-U+001F outright, not even as numeric references, and LibreOffice answers the resulting file with `Error: source file could not be loaded`. So the fate is loud rather than silent, but loud when the reader opens the file, which is the worst moment to find out, and it fails criterion 1 for any design carrying one. The two C0 characters XML does allow were measured separately: a literal tab inside `w:t` renders exactly as `w:tab` does, so marking it would throw away a character the format carries; a line feed inside `w:t` renders as a **space**, and a carriage return is normalised to a line feed by the XML parser before Word ever sees it, so a line ending left alone is a break silently lost, hence a real `w:br` and a Windows line ending read as one break rather than two. An unpaired surrogate, which JSON can carry and XML cannot, is marked too. The count is per piece of the design's own text, so `control-labels.json` reports one and not six: the PDF marks its tabs and its DEL, the Word file carries them | Task 08, 2026-09-18 |
| D77 | `fontCoverage.ts` does not apply to the Word export, and nothing is marked for a font's sake | verified rather than assumed, both ways round. D69 and D70 exist because jsPDF embeds a subset face and writes *nothing* for a glyph outside it, so the loss is silent and the file is the only place it could be caught. A `.docx` embeds no font at all -- confirmed by listing the package, where `word/fontTable.xml` names faces and no font binary is present -- so what a glyph looks like is the reader's machine's question. Marking an arrow, a CJK ideograph or a tick here would turn a character the reader's Word draws perfectly well into a square, which is a loss of fidelity rather than a protection against one, so the export keeps them and the walk asserts that it does. D76's rule is the separate answer to the separate question | Task 08, 2026-09-18 |
| D78 | `openDrawing`/`resolveStyles` move out of `toPdf.ts` into `src/lib/openDrawing.ts`, and the marking walk moves out of `drawableText.ts` into `src/lib/markLayout.ts`; both exports share them rather than keeping a copy | D64 is not svg2pdf's problem, it is the problem of anything outside the page reading the drawing: an SVG handed to an `Image` from a blob URL is parsed as a document of its own, so it has no host page, no stylesheet, and every `var(--shape-*)` in `drawing.module.css` resolves to nothing. The canvas rasterisation needs the same step for the same reason, so the step is shared and the PDF's font override -- which is genuinely the PDF's, and D65's -- arrives as an argument instead of being hard-coded inside it. `markLayout` is the same move on the other seam: the *rule* differs between the two exports (D76 against D69) but the walk over the title, every node's id, label, drawn lines and type, and every edge's ends and label, is identical, and a second copy is a second thing to drift. Both are behaviour-preserving: the whole suite, 343 unit tests and 97 walk steps, is green across the move | Task 08, 2026-09-18 |
| D79 | The Word file's text, tables and metadata are read back out of the produced bytes by `e2e/docxText.ts`, a hand-written zip and XML reader over Node's own `inflateRaw`, and the Word block lives in `e2e/exportWord.spec.ts` | D68's two decisions, applied again for the same reasons. Reading the bytes back is the only honest check, because everything that can go wrong with a `.docx` goes wrong on the way out and reports success on the way in. It is hand-written because it needs three things and no more -- a zip central directory, `inflateRaw`, and enough regular expressions to find `w:t`, `w:br`, `w:tbl` and a handful of attributes -- and the gate on this task was deliberate about which packages get added, so the one that was added is the product's rather than the test suite's. It is strict on purpose: it throws on a zip it does not understand rather than returning nothing, because a reader that quietly finds no text turns a broken export into a passing test. The separate file is the 800-line ceiling, checked rather than estimated: `e2e/export.spec.ts` was 511 lines and `e2e/exportPdf.spec.ts` 540, so a fourth format went into its own 504-line file and the four-download pass, which belongs to the row rather than to any one format, stayed in `export.spec.ts`, now 561 | Task 08, 2026-09-18 |
| D80 | The export row's name is on the page rather than only in an `aria-label`: the group is `aria-labelledby` a visible "Export the design" heading, all four buttons share one minimum width, and the press has a state of its own | D59 named this task as the row's fuller revisit and the fourth button is what made it necessary. Three buttons read as three buttons; four of 15, 11, 10 and 11 characters read as a pile, and a wrap put them on two ragged lines. One minimum width makes them a set whose longest label still fits and keeps the row the same shape whichever design is loaded. The name moving onto the page is strictly better than the attribute it replaces: the group had a name only a screen reader ever heard, and now there is one name rather than two copies that can disagree, with `getByRole('group', { name: 'Export the design' })` still finding the same group by the same words. The row still declares no colour of its own, because the page around it is Task 09's -- the hover and press tints are alpha over whatever is behind them, the transition names its two properties rather than `all`, and `prefers-reduced-motion` turns the press movement off | Task 08, 2026-09-18 |
| D81 | **Overturns the first half of D66**: the drawing is no longer scaled to fit one page. It is printed at a size its own smallest text can be read at, and a design too large for one page at that size is tiled across as many sheets as it needs. D66's second half stands unchanged -- the PDF's drawing is still vector, and both tables still carry every label at full size | D66 said "scaled to fit the page rather than tiled across several" and defended it with "vector is what saves it, since it stays sharp at any zoom". That defence is true of a screen and false of paper, which is the medium a PDF is chosen for: `platform-overview.json`, the fixture built to stand for the owner's own scale, printed its type line at 3.7 pt in the PDF and its node label at 4.25 pt in the Word file, and no zoom helps a printed page. D66 was also made before the Word export existed, so it was made about one format and inherited by two, and before the 1000-node measurement showed the Word raster collapsing to a three-pixel hairline at the same cap. The premise changed rather than the reasoning being wrong, and the reopening was authorised on the record by Jared in the cycle's handoff. Recorded here as a new row rather than an edit to D66, the way D61 corrected D57 | Fix: the drawing's on-page label size, 2026-09-18 |
| D82 | The floor is **6 pt**, judged against `TYPE_FONT_SIZE` (11 px) -- the smallest of the three sizes `layout.ts` draws at -- and not against the 14 px node label. `SMALLEST_TEXT_PX` is imported from `layout.ts` rather than written down again | Both earlier handoffs reported the 14 px label, and a floor set against it would have let the type line print at 4.7 pt while the number on the page said 6. The type line is the only place a reader sees the free text the file actually gave for a node's `type`, so it is the one that decides whether the drawing is writing or ink. Six rather than seven because the cost of a floor is paper and it rises fast: measured on `platform-overview.json`, 6 pt costs three sheets in both formats, 6.5 pt costs three in the PDF and six in the Word file, and 7 pt costs six in both. Six is a real printed size -- a footnote or a legal line -- and at it the node label prints at 7.6 pt and the edge label at 6.5 pt, because those are drawn larger in the same proportion. Importing the 11 rather than repeating it means a change to the drawing's typography moves the floor with it instead of leaving this measuring the wrong thing | Fix: the drawing's on-page label size, 2026-09-18 |
| D83 | The floor and the sheet arithmetic live in one pure module, `src/lib/drawingSheets.ts`, which both planners read; it works in **points per drawing pixel**, and each format passes in its own page and its own `naturalScale` -- 1 for the PDF, 0.75 for the `.docx` | D78's precedent, and the same reason: the decision that was wrong was three lines of `pdfPlan.planDrawing` and three lines of `docxPlan.planDrawing` saying the same thing, agreeing only because nobody had changed one of them. Points because it is the only unit both formats share and the only one a floor can honestly be stated in; a `.docx` measures images in pixels at 96 DPI, so `docxPlan` converts on the way in and out. `naturalScale` is not the same number in both and that is not an oversight -- a PDF places a drawing pixel at a point and a `.docx` at three quarters of one, which is exactly why `markup-labels.json` measured 11.8 pt in the PDF against 10.5 pt in the Word file for the same label. Without it the Word picture came out a third larger than its own size, which is the scaling-up D66 rules out. Being pure is what makes it testable at all: the old arithmetic could only be reached through two modules that need a browser for everything else they do | Fix: the drawing's on-page label size, 2026-09-18 |
| D84 | A drawing is cut into sheets by a greedy pass that pulls any cut falling through a shape back to that shape's own near edge, never advancing less than half a sheet; the same number of sheets is then spread evenly and kept if it leaves at least as many shapes whole. A tiled drawing takes a page per sheet in both formats, captioned with its place in the whole, with the count said before the sheets start | Criterion 5.2 says nothing is dropped and nothing is cut in half, and a uniform overlap wide enough to guarantee that costs far more paper than it needs to: set against the widest shape in `platform-overview.json` it turned the Word file's three sheets into six, because it pays for the worst boundary at every boundary. Pulling back only where a cut actually lands through something pays for it where it happens. The half-sheet limit is what keeps the pass finite when a shape is nearly as wide as a sheet, and a shape larger than a sheet cannot pull a cut at all, because no sheet could hold it whole and its ink appears on every sheet it crosses anyway -- `estate-sweep.json` has an edge whose route is 3,532 px tall against a 1,241 px sheet, and it is drawn across the rows it runs through -- four of its 132 shapes in the PDF and five in the Word file are in that class, every one of them a route rather than a box or a plate. The even-spacing pass is about what a reader sees: the greedy one pulls its last sheet flush to the end of the drawing, which on `platform-overview.json` left sheet 3 starting 25 px after sheet 2 -- two sheets showing very nearly the same picture. A page per sheet rather than sharing the first page with the title, because sharing costs an inch of height on *every* sheet and measured out at twice the total for `platform-overview.json` | Fix: the drawing's on-page label size, 2026-09-18 |
| D85 | **Amends D74**: its "the canvas is bounded because the placed size is capped at one page" no longer holds, and both bounds D74 named as the other route's price are now built -- `MAX_DRAWING_SHEETS` is 16, `MAX_SHEET_RASTER_PIXELS` is one content area at `RASTER_SCALE`, and `MAX_RASTER_PIXELS` is their product. The raster also gains a floor of its own, `MIN_RASTER_SIDE` of 200 px on the short side, raised on both sides together. `toDocx` paints, encodes and releases one sheet at a time | D74 said in as many words that scaling the drawing's own size "is what would have needed a pixel budget and a cap", and this cycle takes that route, so it owes both. Sixteen because `estate-sweep.json` at forty nodes needs twelve, and a cap the largest design anyone has asked for already sits against is one that will be hit by accident; past it the drawing prints below the floor and the document says so, because a two-hundred-sheet PDF is not a document. The raster floor is separate from the sheet cap and neither replaces the other: tiling does not fix a thin drawing, because a drawing a thousand times wider than it is tall is thin on every sheet of it, and the 1000-node chain still places at 16 x 840 px. Measuring the canvas off the placed size alone is what made it a 3 x 2304 PNG; with the floor it is 200 x 10,549. Both sides are raised together, and the budget trims the result, because a picture stretched back into its placed shape is worse than a small one. One at a time because sixteen canvases at 4.85 megapixels each is 78 megapixels held at once, against one held now | Fix: the drawing's on-page label size, 2026-09-18 |
| D86 | **Amends D75 by re-reading rather than repeating it**: the Word page stays US Letter portrait, and the two formats now take different numbers of sheets for the same design -- twelve against fourteen for `estate-sweep.json` -- because each is cut against its own content box | D75 rejected landscape on a measurement -- the drawings this app produces are usually taller than they are wide, so turning the page shrank three fixtures of four to help one. Tiling does not revive the argument, it retires it: a sheet is a window on the drawing rather than the whole of it, so the page's shape now decides how the window is cut rather than how far the picture is crushed, and the canvas grows without bound in whichever direction the graph runs (6,480 px tall at forty nodes, 17,210 px wide at two hundred) so no single orientation is right. The two formats differing is honest rather than a drift: the PDF's content box is 516 x 696 pt against the Word file's 468 x 648, because D66 chose a 48 pt margin and D75 chose an inch, and forcing one to the other's geometry would undo a decision made deliberately. They agree where it matters -- both print `platform-overview.json` on three sheets at exactly 6.00 pt | Fix: the drawing's on-page label size, 2026-09-18 |
| D87 | A sheet of a tiled drawing is named to a screen reader rather than described again: the first sheet's alt text is its caption followed by the whole `describeDrawing` text, and every sheet after it carries its caption alone. The PDF's caption is set in the rules' grey rather than the document's ink | Best-effort accessibility is this project's standard (`docs/ARCHITECTURE.md`) and reading the same fourteen-node description out fourteen times buries the one thing that differs between the fourteen pictures, which is which piece of the drawing each holds. The whole description still exists once, at the front, where a reader meets it first. The grey is the same judgement in ink: a caption in the document's own near-black competes with the table rows further down the page, and a line naming the picture above it should lose that competition | Fix: the drawing's on-page label size, 2026-09-18 |
| D88 | The printed size is read back out of the produced bytes: `e2e/pdfText.ts` gains `pdfDrawnText`, which walks a content stream tracking `q`, `Q`, `cm` and `Tm` and reports each run's nominal size beside the size it prints at; `e2e/docxText.ts` gains `docxPictures`, which reads each PNG's own IHDR beside the `wp:extent` it is placed at. A `.docx` whose drawing is cut along both axes can only be measured as a lower bound, and that case is the PDF's to prove exactly | D68 and D79's rule, applied to the number this cycle turns on: a plan that says 6 pt and a file that draws 3.5 pt is exactly the failure that survived two tasks of review. Nominal size is not enough on its own -- svg2pdf writes a 6 pt type line into the file as `/F15 11 Tf` with a text matrix of 0.545, so reading the operand would report the drawing at its own pixel sizes however far it had been shrunk, which is how the opening table came to report the PDF's 12 px edge label while believing it was the 14 px node label. The Word file is a raster and carries no font size at all, so its scale is recovered from the placed size against the drawing: exact along any axis the drawing was not cut along, because a cut axis always fills the sheet exactly, and a lower bound otherwise -- which is the safe direction, since understating can only fail a file that should have passed. `estate-sweep.json` is cut both ways and so reads as a bound of 4.86 pt against a true 6.00; the PDF measures the same design at 6.00 pt exactly and the unit tests hold the arithmetic | Fix: the drawing's on-page label size, 2026-09-18 |
| D89 | **Amends D84 by distinguishing the two reasons a shape can fail to be whole on a sheet.** Four of `estate-sweep.json`'s shapes in the PDF and four of its five in the Word file are routes larger than any sheet, which by construction cannot pull a cut and are drawn across every sheet they cross. The Word file's fifth exception is not in that class: a route at x 306.3-470.5, y 2366.4-3298.1 (164 x 932 px) fits inside an 858 x 1144 px sheet, but `MIN_SHEET_ADVANCE`'s half-sheet floor forces the cut past it anyway -- the greedy pass wants to pull the cut back to 2366.4 to keep the route whole, but that is only 240 px of advance from the previous cut at 2126, under the 572 px floor, so it is forced forward to 2698, past the route's start | Verification for this cycle found the clause in D84 -- "four of its 132 shapes in the PDF and five in the Word file are in that class" -- read as though all five Word exceptions were shapes too large for any sheet. One is not. The distinction matters because the two reasons are different trade-offs: "too large for any sheet" is unavoidable, while "fits but is cut anyway" is the price of `MIN_SHEET_ADVANCE`'s half-sheet floor, which is what keeps the sheet count finite and so protects both the 16-sheet cap (D85) and the 6 pt floor (D82) it exists to guarantee. That trade is judged defensible and is not changed by this row -- only the record is corrected | Fix: the drawing's on-page label size, round 1 verification, 2026-09-18 |
| D90 | The published JSON Schema is generated by Zod's own `z.toJSONSchema`, written to `public/design.schema.json` by `bun run schema`, and compared against a fresh generation by `publishedSchema.test.ts` | Task 09's step 1 says generate rather than write twice, and the generation method was a confirmation gate because it depends on the Zod version. The installed Zod is 4.6.5 and already exports `z.toJSONSchema`, so the best possible answer against D7 and D8 was available: **no new dependency at all**. It emits clean draft 2020-12, and D39's blank-string rule survives as `minLength: 1` plus a `\S` pattern - the rule was deliberately written as two field checks rather than as a `refine` for exactly this reason, and that has now paid. The artifact is a file on disk rather than a build-time endpoint because "published" means a URL a validator can be pointed at *and* a file a reader can see in the repository; the price is a second copy, and it is paid the way D57 pays it, with a test that regenerates and compares rather than with care. The comparison is of parsed JSON rather than of text, because the formatter owns the whitespace and the whitespace is not the promise, and the failure message names `bun run schema` so the test says how to fix what it found | Task 09, 2026-09-18 |
| D91 | The document is generated with `io: 'input'`, not Zod's default output side, so `additionalProperties` is omitted from it entirely | Zod defaults to `io: "output"`, which describes the object it hands *back* - and that object has had unnamed keys stripped, so the emitted document says `additionalProperties: false` at every level. D19 says the opposite and means it: a property the schema does not name is **ignored, not rejected**, and parsing `{ title, nodes, edges, extra }` really does return the three keys and draw. Publishing the default would put a promise in a file that the running code does not keep, and an external validator pointed at it would refuse files this app loads. Both alternatives were put to the user and both were refused: tightening the app to match the stricter schema would overturn D19 and break files that work today, and shipping the default with a prose caveat would mean a published artifact that disagrees with the running code and a caveat nobody reads. `io: 'input'` omits the keyword, and the published file then accepts exactly what the app accepts | Task 09, 2026-09-18 |
| D92 | Neither `superRefine` rule survives generation, so both are carried in prose on `/schema` beside the file, along with D20's two-pass reality, and the artifact is never hand-edited | Unique node ids and edges naming defined nodes leave no trace in the serialised output, and a test now pins that absence rather than leaving it as something someone once observed. JSON Schema has no vocabulary for a rule that reads one field against another, so this is a real gap and not a setting. Post-processing the document to hand-add them was put to the user and refused, and rightly: the moment a generated artifact is edited by hand it is a second definition, which is the drift this module exists to prevent. So the file stays machine-generated and the page carries the two as `RULES_THE_FILE_CANNOT_STATE`, clearly marked as rules the file cannot state, together with `TWO_PASS_NOTE` - because D42 already promises users that the panel says what *that pass* found, and a published page that left that out would be quietly less honest than the error panel the app already ships | Task 09, 2026-09-18 |
| D93 | The sample is `public/sample.json` and that is the only copy of it: the site's asset, the text the `/schema` page prints, the file the "Load the sample design" button fetches, and the fixture the Playwright walk chooses. It demonstrates no type the app does not recognise | Four uses, one file. Static assets live in `public/` and fixtures live in `e2e/fixtures/`, so the lazy answer was two files with the same bytes - and two files with the same bytes is precisely the class of problem the next cycle exists to clean up (D51's sentence in four files, the export column headings in four). `e2e/pages/schemaPage.ts` exports `shippedSamplePath`, and everything that wants the file comes there for it. On the second half: the sample carries all six canonical kinds and a label on every edge, and deliberately no unrecognised `type`. The grey dashed fallback is real and worth exercising, but `e2e/fixtures/order-intake.json` already exercises it with `worker` and `widget-factory`; a *sample* is an example people copy, and teaching a reader to write a word that gets them a box with nothing claimed is teaching them the wrong thing | Task 09, 2026-09-18 |
| D94 | Both pages share `src/layouts/PageLayout.astro` and `src/styles/page.module.css`; the page's colour and type tokens are declared on the body's own module class rather than on `:root`, and `upload.module.test.ts` now measures the panel against `--page-surface` instead of against `#ffffff` | The decision Tasks 03 and 06 parked here in their own stylesheet comments, taken once now that there are two screens. Direction: a technical document rather than a product page - warm paper (`#fbfaf8`) against the drawing's cool neutral, so a drawing reads as a plate laid on the page; the drawing's own `--shape-text` as the page's ink, so the words above a diagram and the words in it are one colour; hairlines rather than cards; and monospace reserved for the things that are literally in a user's file, which is the same face the validation panel quotes them in. D1 is not reopened - declaring the tokens on `.page` rather than on `:root` keeps this an ordinary CSS Module with no global escape hatch, and custom properties still inherit down into `upload.module.css` and `exports.module.css`, which are its descendants in the DOM. Two pages is also where one `<head>` written twice starts to cost, which is what the layout is for. The contrast tests moved with the colours rather than being left measuring a background the page no longer has: `page.module.test.ts` measures every ink against both the page and the plate, and the panel's bare-page measurement reads the real surface out of the page stylesheet | Task 09, 2026-09-18 |
| D95 | "Load the sample design" is a `<button>`, not a link, and it fetches `/sample.json` at run time and hands the text to `loadDesign`, without `looksLikeJsonFile` | A control that does not navigate is a button; a link that does not navigate is a promise broken for anyone who was told it is a link, and the sample's real URL is offered honestly as a link on `/schema` instead. Fetched at run time rather than imported at build time, so the bytes the button loads are the bytes the site serves and the ones the download link points at. It goes through `loadDesign` because a sample that skipped validation could ship broken and draw anyway, and a sample that failed validation is a bug the page should say out loud rather than route around - a non-ok response is turned into an error first, because `fetch` would otherwise hand the 404 page's text to `JSON.parse` and the panel would report a syntax error in a file the user never wrote. It is *not* run through `looksLikeJsonFile`: D40 exists because a **dropped** file never met the input's `accept`, and an asset the site itself ships is not that case. `holdDrawing` sets the drawing, the held layout and all four export buttons together (D53), and the status line names `sample.json` like any other file (D41) | Task 09, 2026-09-18 |
| D96 | The `/schema` page inlines the sample with Vite's `?raw` rather than reading it with `readFileSync(new URL(..., import.meta.url))` | Recorded because the obvious route is wrong in a way only the build shows. `import.meta.url` in Astro frontmatter points at the source file under `bun run dev` and at the bundled chunk under `astro build`, so the page rendered perfectly in development and then failed the build looking for `dist/public/sample.json`. `?raw` is resolved by Vite at compile time, so there is nothing left to resolve at run time. This is not a contradiction of D61, which is about `?raw` on a **CSS Module under Vitest with `css: false`**, where it comes back as a proxy stub; this is a `.json` file in Astro's own build, and the page is covered by Playwright rather than by Vitest. `e2e/schema.spec.ts` compares the text on the page against `public/sample.json` on disk, so the inlining cannot drift from the file | Task 09, 2026-09-18 |
| D97 | `e2e/staticServer.ts` answers a request that resolves to a directory with that directory's own `index.html` | `astro build` writes `/schema` as `dist/schema/index.html`, and the walk's server refused it: a directory is not a file, so `/schema` was a 404 while a static host would have served the page. Serving a directory's index is what a static host does, so the walk now tests what production does. It is not the single-page fallback the file still refuses in its own comment - a URL that names nothing is still a 404, because the `stat` of a missing `index.html` throws into the same catch | Task 09, 2026-09-18 |
| D98 | Dagre 3.1.1 cannot lay out a graph that carries both a two-cycle and a parallel duplicate between the same pair of nodes, so `layoutDesign` asks it twice: once with one dagre edge per edge in the file, and if that does not come back whole, again with one dagre edge per pair of nodes, with `parallelEdges.ts` fanning the duplicates apart afterwards. "Whole" means no throw *and* every coordinate finite. D58's claim that `straightLine` is unreachable because `assignNodeIntersects` always completes is corrected in place; the function is unreached for a different reason | Diagnosed rather than guessed. Dagre's ordering pass has a special case that merges two parallel dummy chains with equal barycentres when one of them is a reversed edge, which is what a two-cycle becomes after dagre's own acyclic pass. The merge drops one chain out of the ordering, so that dummy is never given a position and comes back holding `NaN`. It surfaces two ways: where the `NaN` is an edge's first or last point, `assignNodeIntersects` throws `Not possible to find intersection inside of the rectangle`; where it is not, dagre returns normally and the `NaN` lands in the path's `d`, which voids the whole path and drops the edge silently. The second half was found by fuzzing during this cycle and is the worse of the two, because nothing announces it. Merging the parallel duplicates removes the shape the special case fires on: across 60,000 fuzzed multigraphs of 2 to 8 nodes over three seeds, per-edge keying failed 36 times (34 throws and 2 silent `NaN`s) and per-pair keying failed zero times, while `acyclicer: "greedy"` left 17 of 20 of those failures in place and was rejected for changing every existing drawing as well. Per-edge keying is tried first and is provably unchanged - every group holds one edge, so the reserved plate is that edge's own plate, the route is returned as given and the label centre is returned as given - which is why no existing drawing moves. Not chosen: patching or forking dagre (the handoff forbids pinning to one, and a fork is a maintenance cost this project cannot carry), adding a second graph library (same), and making the merged keying the only keying (it would redraw every parallel edge in every existing design to fix one in thousands). A design that neither keying places throws `DesignLayoutError`, which `describeLoadError` words as the app's own limit rather than quoting dagre - a floor, not the fix | Fix: the layout crash and the loader's wording, 2026-09-18 |
| D99 | `describeSyntaxFault` reads a second engine clause, `at line N column M of the JSON data`, and takes SpiderMonkey's line and column as the engine gave them rather than counting them from the file. Both clauses live in one ordered list, `ENGINE_POSITION_CLAUSES`, that `placeIn` and `engineDetail` both walk, so the position that is printed and the position that is stripped from the detail can never come from different clauses. V8's clause stays first, stays end-anchored and is otherwise untouched | On Firefox the panel said "this browser did not say where in it" and then quoted an engine message naming a line and a column. The cause was the shape of the return type, not the pattern: `positionIn` could only yield a character index, and SpiderMonkey gives a line and a column and no index, so its message could not be represented and fell to the no-position branch. Yielding a `LineAndColumn` from the second clause lets both reach `atPosition` unchanged. Taking the engine's own numbers does not break rule 1 - a line inferred from nothing is forbidden, and this line is the engine's own statement about its own parse, which is the only thing that rule ever allowed a line to be read from. End-anchoring the new clause is insurance rather than a measured need: Firefox 156.0 was asked what it says about a file whose own text reads `position 900` and it quotes no part of the file at all, unlike V8's no-position form, which is what D32's anchor exists for. Every SpiderMonkey string in `describeLoadError.test.ts` was read out of Firefox 156.0 on this machine over Marionette rather than taken from documentation, and the trailing-comma case is the same file V8's case uses, so the two engines are asserted to reach the same line and column. V8's and JavaScriptCore's wording is unchanged, including the no-position sentence, which still appears for an engine that genuinely names none | Fix: the layout crash and the loader's wording, 2026-09-18 |
| D100 | D98's "per-edge keying failed 36 times in 60,000" and Jahmyr's independent verification finding of 581 failures in 60,000 are both correct and do not contradict each other | The two runs used different generators. D98's figure is Amon's: 2 to 8 nodes, up to 2n edges. Jahmyr's 581 (531 throws and 50 silent `NaN`s, including whole node boxes coming back `x: NaN, y: NaN`) came from an independently written, denser generator: 2 to 10 nodes, up to 3n edges. Per-pair keying found zero failures against either generator. Recorded so a reader who finds both numbers later does not read them as a discrepancy in the fix rather than a difference in what was fuzzed | Fix: the layout crash and the loader's wording, round 1 verification, 2026-09-18 |
| D101 | `src/lib/layout.test.ts`'s 2500-graph fuzz (`lays out 2500 random multigraphs without one failure`) gets its own `FUZZ_TIMEOUT = 60_000`, up from Vitest's 5s default. Count, seed, generator and assertion are untouched | The test has no logic bug: it ran 3-4s locally but 5022ms on a GitHub Actions runner, which failed the push-event CI run of PR #23 while the pull_request-event run of the identical commit passed. A verdict that depends on which runner picks up the job is a flaky wait, not a real failure, and it is the first flaky-CI finding recorded in this project | Fix: the layout crash and the loader's wording, round 1 verification, commit dbbfca5, 2026-09-18 |
| D102 | The words an export supplies itself — the empty-design sentence (D51) and both tables' column headings, each paired with the share of the width it takes — are declared once, in `src/lib/exportFurniture.ts`. `pdfPlan.ts` re-exports `NOTHING_TO_DRAW` under that name rather than declaring a second one, and `toHtml.ts` and `toMarkdown.ts` derive their headings off the same lists through a tuple-preserving `headingsOf`, keeping the exact `readonly ['Id', 'Label', 'Type']` type they used to declare by hand | The sentence was written out four times and the six column shares twice, behind identical private `Column` interfaces in both planners. One Vitest assertion held two of the four sentences together; `toHtml.ts` and `docxPlan.ts` were covered by nothing, so half the copies could drift silently. **D66's reasoning was thinner than it reads**: its claim that "a third literal copy cannot drift" rests on a test comparing the PDF's copy with the Markdown export's, which covered three copies and never the fourth. After this cycle there is no second copy to compare, so that guarantee is structural rather than asserted, and `exportFurniture.test.ts` pins the one copy the way `exportStyles.test.ts` pins the palette. The new module is a *sibling* of `exportStyles.ts`, not an extension: that one holds a duplicate that has to be kept honest, this one holds the original, and merging them would blur why either exists. D71 is untouched — the furniture is English whatever language the design is written in, nothing here takes a locale, and the doc comment says so. Proved rather than asserted: `toMarkdown`, `htmlPage`, `pdfPlan` and `docxPlan` were captured for all 11 loadable e2e fixtures before and after the moves and came back byte-identical, same MD5 | Chore: one copy of the export furniture, round 1, 2026-09-18 |
| D103 | A fixture's name says either *no bytes* or *no nodes*, never both. `e2e/fixtures/empty.json`, which is 0 bytes, is now `empty-file.json`, beside the `empty-design.json` that holds a valid design with nothing in it | One word was carrying two opposite cases. `validation.spec.ts` chose `empty.json` to prove the app says "That file is empty." — a file with no bytes, which is a syntax error — while `describeUpload.test.ts` passed the same string as a file *name* beside a design of 0 nodes and 0 edges, which is valid and draws fine. `export.spec.ts` then needed three lines of comment to tell a reader which fixture was which, which is a cost the names should have been paying themselves; that comment is gone. Renamed with `git mv`, so `git log --follow` still reaches the fixture's first commit; it still measures 0 bytes, and no other file in `e2e/fixtures/` was renamed, added or removed | Chore: one copy of the export furniture, round 1, 2026-09-18 |
| D104 | The LibreOffice check is one opt-in command, `bun run docx:libreoffice`, whose Playwright run lives in a second config — `playwright.libreoffice.config.ts`, `testDir` `scripts/libreoffice/` — rather than in a project or a tagged spec under `e2e/`; `vitest.config.ts` also collects `scripts/**/*.test.ts` so the check's pure half runs in the suite that runs everywhere | CI runs `bun run test:e2e`, which is `playwright test`, which runs every project in the default config over everything under `e2e/`. Anything added there runs in CI by default, and this needs an office suite CI has not got. A tag or a skip would keep it out only as long as it fired correctly on every machine, which is the class of thing this cycle exists to stop relying on; a config `playwright test` never reads cannot be reached at all. D31's reasoning, one step further: heavy things get their own entry point | Chore: a repeatable LibreOffice check for the `.docx` export, round 1, 2026-09-19 |
| D105 | What a conversion meant is a pure function of three facts — whether it timed out, its exit code, and whether the output file was written. stderr is carried as diagnostic text and never decides a verdict | A successful conversion writes `Could not find platform independent libraries <prefix>` to stderr every single time on this machine. It is a Python-embedding warning with nothing to do with the document, so a check reading stderr as failure reports every good run as a bad one — the most likely way this work could have shipped broken. Exit 0 is not sufficient either: LibreOffice answers a file it cannot parse with a message and an exit code of 0, which is exactly the shape D76's control characters produced, so the missing file is the only honest signal. Pure, so `verdict.test.ts` pins every rule in Vitest on a machine with no LibreOffice at all, CI included | Chore: a repeatable LibreOffice check for the `.docx` export, round 1, 2026-09-19 |
| D106 | Presence is answered by looking for the binary's file, never by running it. `soffice --version` is not used, and every invocation that does run is bounded by a 120-second timeout reported as a timeout naming the command, distinct from a conversion that failed | `--version` hangs even with a profile directory of its own — tried twice in this cycle, both killed at the timeout — so the natural presence probe is the one thing that cannot be used as one. A timeout and a failure also want different things done about them: a failure is a document that will not convert, a timeout is usually an invocation missing its profile flag, and a message that does not say which leaves the next person to work it out again | Chore: a repeatable LibreOffice check for the `.docx` export, round 1, 2026-09-19 |
| D107 | The binary is located by `MAPDS_SOFFICE` first and then by full per-platform paths; on Windows that is `soffice.com` and never `soffice.exe`, and an override pointing at nothing is an answer rather than a fallback | Three things measured here: the `.exe` is the GUI binary with no console attached, so its output goes nowhere; LibreOffice is not on `PATH`, so it has to be found by path rather than by name; and `C:\Program Files (x86)\LibreOffice Maintenance Service\` sits beside the real install and is an updater, which a search matching on the name "LibreOffice" would find. The override comes first so a teammate on macOS or Linux is not locked out by a list written on one machine, and refusing to fall back from it is what makes the no-LibreOffice case rehearsable on a machine that has one | Chore: a repeatable LibreOffice check for the `.docx` export, round 1, 2026-09-19 |
| D108 | Every invocation passes `-env:UserInstallation=file://…` pointing at a profile directory the run owns, made under the system temporary directory along with the exported and converted files | Without the flag `--convert-to` hangs indefinitely: LibreOffice wants a user profile and waits on the one the desktop session already holds. That was Task 08's hard-won note and is now in the argument list with a test on it. Under the temporary directory rather than the repository so that no profile, export or conversion can land in the working tree — `git status` after a full run is clean by construction and `.gitignore` needed no new line. A room is kept rather than removed when its fixture fails, because the file is the first thing anyone will want | Chore: a repeatable LibreOffice check for the `.docx` export, round 1, 2026-09-19 |
| D109 | What the converted document must still say is derived from the fixture's own JSON through the export's `safeDocxText`, not from the plan `docxPlan` built | Asking the exporter what it meant to write and then confirming the file says that would pass a build that dropped every label, as long as it dropped it in both places. The file the user wrote is the only source that is not downstream of the bug. The single permitted difference between the two is D76's marking of characters XML 1.0 has no room for, which is applied through the export's own function rather than reimplemented, so the check cannot drift from the rule it is checking | Chore: a repeatable LibreOffice check for the `.docx` export, round 1, 2026-09-19 |
| D110 | `MAPDS_SOFFICE` is documented in `README.md` and deliberately not added to `.env.example` | `.env.example` is generated from `docs/intake.md` and describes what the *app* needs; its current text says MapDataStructures reads nothing from the environment, and that stays true — this variable is read by one local developer command and by nothing the site ships. A line there would be lost on the next regenerate and would tell a reader the browser reads an environment it never sees. Recorded rather than done quietly, because `CLAUDE.md` says every new environment variable gets a placeholder and this is the exception to it | Chore: a repeatable LibreOffice check for the `.docx` export, round 1, 2026-09-19 |
| D111 | The check's conversion-failure message is a pure module of its own, `scripts/libreoffice/failureMessage.ts`, and it carries **both** of LibreOffice's streams, each quoted under the name of the stream it came from, with neither one summarised nor filtered | Round 1 built the message from stdout alone, inside `docxRoundTrip.spec.ts` where no test could reach it — that file imports `@playwright/test` and needs an office suite installed, so the one piece of this check with no test on it was the piece nobody reads until something is already wrong. LibreOffice explains a refusal on stderr and is silent on stdout while it does it: measured here on a truncated export, exit 1 with stdout `""` and stderr carrying `Could not find platform independent libraries <prefix>` and `Error: source file could not be loaded` underneath it. So on exactly the failure this check exists to catch — D76's shape, which is why `control-labels` is a fixture — the reader was told "LibreOffice printed nothing on stdout" while the diagnosis sat unread in `verdict.diagnostics`, breaking the promise D105 makes about where stderr goes. The same drop misreported a second case: `MAPDS_SOFFICE` pointing at a directory makes `spawn` emit `error`, `exitCode` resolves `null` and the reason reads "was killed before it finished", while the text that says otherwise — `Executable not found in $PATH` — was the part being discarded. Both streams are now required fields on one interface, so a caller that forgets one is a type error rather than a quiet half-diagnosis, and neither is filtered: every successful run writes the platform-libraries warning to stderr, and a filter that knows which lines matter would be D105's mistake in a smaller form | Chore: a repeatable LibreOffice check for the `.docx` export, round 2, 2026-09-19 |
